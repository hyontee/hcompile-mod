--- /mnt/data/used_vehicle_market_patch/gamemodes/laird.pwn	2026-06-16 11:56:55.686368077 +0000
+++ /mnt/data/used_vehicle_govsell_patch/gamemodes/laird.pwn	2026-06-16 13:03:05.220978060 +0000
@@ -29627,33 +29627,7 @@
 {
     if(response)
     {
-        new vehicleid = GetPlayerOwnableCar(playerid);
-        if(vehicleid != INVALID_VEHICLE_ID)
-        {
-            new model_id = GetVehicleData(vehicleid, V_MODELID);
-            new car_price, sell_price;
-
-            if(GetVehicleJsonCostByModelId(model_id, car_price))
-            {
-                sell_price = car_price / 2;
-
-                if(DestroyOwnableCar(vehicleid))
-                {
-                    new fmt_str[256];
-
-                    SetPlayerData(playerid, P_OWNABLE_CAR, INVALID_VEHICLE_ID);
-                    GivePlayerMoneyEx(playerid, sell_price, "Продажа тс в госс", true, false);
-
-                    format(fmt_str, sizeof fmt_str, "{FFFFFF}На ваш баланс было выдано {FF5252}%d рублей (50%% от гос. цены)", sell_price);
-                    SendClientMessage(playerid, -1, "{FFFFFF}Вы успешно продали транспорт государству.");
-                    SendClientMessage(playerid, -1, fmt_str);
-                }
-            }
-            else
-            {
-                SendClientMessage(playerid, -1, "{00CC00}Ошибка: цена для этого транспортного средства не найдена в базе данных");
-            }
-        }
+        return UVM_SellOwnVehicleToState(playerid);
     }
 }
 
@@ -54982,57 +54956,7 @@
 
 CMD:sellcar(playerid, params[])
 {
-    new vehicleid = GetPlayerOwnableCar(playerid);
-    if(vehicleid != INVALID_VEHICLE_ID)
-    {
-        new model_id = GetVehicleData(vehicleid, V_MODELID);
-        if(model_id)
-        {
-            new fmt_str[999];
-            new price, sell_price;
-            new vehicle_name[64];
-
-            if(!GetVehicleJsonNameByModelId(model_id, vehicle_name, sizeof(vehicle_name)))
-                format(vehicle_name, sizeof(vehicle_name), "Неизвестный транспорт");
-
-            if(GetVehicleJsonCostByModelId(model_id, price))
-            {
-                sell_price = price / 2;
-
-                format
-                (
-                    fmt_str, sizeof fmt_str,
-                    ""SC"Транспортное средство:\t{FF5252}%s (№%d)\n"\
-                    ""SC"Стоимость продажи:\t{FF5252}%d руб.\n\n"\
-                    "{FFFFFF}Вы можете продать свое транспортное средство другим\n"\
-                    "игрокам с помощью команды: {FF5252}\"/sellmycar\".\n\n"\
-                    "{FFFFFF}Вы уверены, что хотите моментально продать свое\n"\
-                    "транспортное средство?",
-                    vehicle_name,
-                    model_id,
-                    sell_price
-                );
-
-                new title[128];
-                format(title, sizeof(title), "{FF5252}%s -> {ffffff}Продажа транспорта", NameServer[SILE_NAME]);
-
-                Dialog(playerid, DIALOG_OWNABLE_CAR_SELL, DIALOG_STYLE_MSGBOX, title, fmt_str, "Да", "Отмена");
-            }
-            else
-            {
-                ShowNotificationSile(playerid, 2, 6, 0, 0, "Ошибка: цена для этого транспорта не найдена в базе данных", "");
-            }
-        }
-    }
-    else
-    {
-        if(GetPlayerOwnableCars(playerid) == 0)
-            ShowNotificationSile(playerid, 2, 6, 0, 0, "У Вас нет личного транспорта", "");
-        else
-            ShowNotificationSile(playerid, 2, 6, 0, 0, "Ваш личный транспорт не загружен на сервере", "");
-    }
-
-    return 1;
+    return UVM_ShowGovSellDialog(playerid);
 }
 
 CMD:sellmycar(playerid, params[])
