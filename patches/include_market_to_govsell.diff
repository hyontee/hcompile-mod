--- /mnt/data/used_vehicle_market_patch/include/system/used_vehicle_market_ext.inc	2026-06-16 12:21:46.464410820 +0000
+++ /mnt/data/used_vehicle_govsell_patch/include/system/used_vehicle_market_ext.inc	2026-06-16 13:03:44.543982862 +0000
@@ -4,43 +4,38 @@
 #define _USED_VEHICLE_MARKET_EXT_INCLUDED
 
 /*
-    Used Vehicle Market + Mileage/Wear/Insurance extension
-    Commands:
-        /usedcars              - list cars listed by players
-        /buyusedcar [db id]    - buy a listed car
-        /sellusedcar [price]   - list loaded personal car on the used market
-        /delusedcar            - remove loaded personal car from the market
-        /carpassport           - mileage/wear/insurance passport for loaded personal car
-        /buyinsurance [days]   - buy insurance for 1..30 days
-        /repairwear            - repair technical wear of loaded personal car
-*/
+    Vehicle mileage + wear + insurance + government sale extension.
 
-#define UVM_DIALOG_LIST          (28100)
-#define UVM_DIALOG_INFO          (28101)
-#define UVM_DIALOG_BUY_CONFIRM   (28102)
+    This version does NOT add player-to-player used market commands.
+    It is made for the existing /sellcar command in laird.pwn:
+    /sellcar opens the government sale dialog, price depends on:
+        - default government vehicle price
+        - mileage
+        - technical wear
+        - active insurance
+
+    Required patch in laird.pwn:
+        CMD:sellcar -> return UVM_ShowGovSellDialog(playerid);
+        DIALOG_OWNABLE_CAR_SELL positive response -> UVM_SellOwnVehicleToState(playerid);
+*/
 
-#define UVM_MARKET_LIMIT         (20)
-#define UVM_MIN_PRICE            (3000)
-#define UVM_MAX_INSURANCE_DAYS   (30)
 #define UVM_WEAR_MAX             (100)
 #define UVM_WEAR_KM_STEP         (10.0)
-#define UVM_SELLER_PERCENT       (95) // 5% market commission
+#define UVM_MAX_INSURANCE_DAYS   (30)
+
+// Sale balance settings
+#define UVM_GOV_SELL_PERCENT         (50) // clean car: 50% of gov price
+#define UVM_MIN_GOV_SELL_PERCENT     (10) // worst car: not lower than 10% of gov price
+#define UVM_MAX_MILEAGE_PENALTY      (35) // max -35% from base sell price
+#define UVM_MAX_WEAR_PENALTY         (60) // max -60% from base sell price
+#define UVM_MAX_TOTAL_PENALTY        (75) // max total penalty from base sell price
+#define UVM_INSURANCE_BONUS          (5)  // active insurance can restore +5% of base sell price
 
 new g_uvm_car_wear[MAX_VEHICLES];
 new g_uvm_insurance_until[MAX_VEHICLES];
 new g_uvm_insurance_claims[MAX_VEHICLES];
 new Float:g_uvm_wear_last_mileage[MAX_VEHICLES];
 
-stock UVM_Min(value, limit)
-{
-    return value < limit ? value : limit;
-}
-
-stock UVM_Max(value, limit)
-{
-    return value > limit ? value : limit;
-}
-
 stock UVM_GetPlayerByAccount(account_id)
 {
     foreach(new i : Player)
@@ -52,27 +47,12 @@
     return INVALID_PLAYER_ID;
 }
 
-stock UVM_GetOwnableIndexBySql(oc_id)
-{
-    for(new idx; idx < MAX_OWNABLE_CARS; idx++)
-    {
-        if(GetOwnableCarData(idx, OC_SQL_ID) == oc_id) return idx;
-    }
-    return -1;
-}
-
-stock UVM_FindLoadedVehicleBySql(oc_id)
+stock UVM_GetBaseVehiclePrice(modelid)
 {
-    for(new vehicleid = 1; vehicleid < MAX_VEHICLES; vehicleid++)
-    {
-        if(!IsValidVehicleID(vehicleid)) continue;
-        if(!IsAOwnableCar(vehicleid)) continue;
-
-        new index = GetVehicleData(vehicleid, V_ACTION_ID);
-        if(index < 0) continue;
-        if(GetOwnableCarData(index, OC_SQL_ID) == oc_id) return vehicleid;
-    }
-    return INVALID_VEHICLE_ID;
+    new price = GetVehicleMarketPriceByModel(modelid);
+    if(price <= 0) GetVehicleJsonCostByModelId(modelid, price);
+    if(price <= 0) price = 100000;
+    return price;
 }
 
 stock UVM_ClearRuntime(vehicleid)
@@ -188,20 +168,15 @@
 
 stock UVM_GetInsurancePrice(modelid, days)
 {
-    new base_price = GetVehicleMarketPriceByModel(modelid);
-    if(base_price <= 0) base_price = 100000;
-
+    new base_price = UVM_GetBaseVehiclePrice(modelid);
     new price_per_day = base_price / 250; // 0.4% per day
     if(price_per_day < 1500) price_per_day = 1500;
-
     return price_per_day * days;
 }
 
 stock UVM_GetRepairWearPrice(modelid, wear)
 {
-    new base_price = GetVehicleMarketPriceByModel(modelid);
-    if(base_price <= 0) base_price = 100000;
-
+    new base_price = UVM_GetBaseVehiclePrice(modelid);
     new price = (base_price * wear) / 700; // about 14.2% of base price at 100 wear
     if(price < 2500) price = 2500;
     return price;
@@ -209,11 +184,11 @@
 
 stock UVM_GetWearText(wear, output[], size = sizeof output)
 {
-    if(wear < 20) format(output, size, "Excellent");
-    else if(wear < 45) format(output, size, "Good");
-    else if(wear < 70) format(output, size, "Worn");
-    else if(wear < 90) format(output, size, "Bad");
-    else format(output, size, "Critical");
+    if(wear < 20) format(output, size, "excellent");
+    else if(wear < 45) format(output, size, "good");
+    else if(wear < 70) format(output, size, "worn");
+    else if(wear < 90) format(output, size, "bad");
+    else format(output, size, "critical");
     return 1;
 }
 
@@ -233,232 +208,113 @@
     return 1;
 }
 
-stock UVM_ShowPassport(playerid, vehicleid)
+stock UVM_GetGovSellPriceEx(vehicleid, &base_price, &clean_sell_price, &mileage_penalty, &wear_penalty, &insurance_bonus, &final_price)
 {
-    if(vehicleid == INVALID_VEHICLE_ID || !IsAOwnableCar(vehicleid))
-        return SendClientMessage(playerid, 0xCECECEFF, "Load your personal vehicle first. (/car)");
-
-    new index = GetVehicleData(vehicleid, V_ACTION_ID);
-    if(index < 0) return 1;
-
-    if(GetOwnableCarData(index, OC_OWNER_ID) != GetPlayerAccountID(playerid))
-        return SendClientMessage(playerid, 0xCECECEFF, "This vehicle does not belong to you.");
+    if(!IsValidVehicleID(vehicleid)) return 0;
+    if(!IsAOwnableCar(vehicleid)) return 0;
 
     new modelid = GetVehicleData(vehicleid, V_MODELID);
-    new vehicle_name[64];
-    if(!GetVehicleJsonNameByModelId(modelid, vehicle_name, sizeof vehicle_name))
-        format(vehicle_name, sizeof vehicle_name, "Unknown vehicle");
+    base_price = UVM_GetBaseVehiclePrice(modelid);
+    clean_sell_price = (base_price * UVM_GOV_SELL_PERCENT) / 100;
 
-    new wear_text[24], insurance_text[32], text[1024];
-    UVM_GetWearText(g_uvm_car_wear[vehicleid], wear_text, sizeof wear_text);
-    UVM_FormatDaysLeft(g_uvm_insurance_until[vehicleid], insurance_text, sizeof insurance_text);
-
-    format(text, sizeof text,
-        "Vehicle:\t%s (#%d)\n"\
-        "DB ID:\t%d\n"\
-        "Mileage:\t%d km\n"\
-        "Wear:\t%d/100 (%s)\n"\
-        "Insurance:\t%s\n"\
-        "Insurance claims:\t%d\n"\
-        "Gov. price:\t%d rub\n\n"\
-        "Commands:\n"\
-        "/sellusedcar [price] - list this vehicle on used market\n"\
-        "/buyinsurance [days] - buy insurance\n"\
-        "/repairwear - repair technical wear",
-        vehicle_name,
-        modelid,
-        GetOwnableCarData(index, OC_SQL_ID),
-        floatround(GetVehicleData(vehicleid, V_MILEAGE), floatround_floor),
-        g_uvm_car_wear[vehicleid],
-        wear_text,
-        insurance_text,
-        g_uvm_insurance_claims[vehicleid],
-        GetVehicleMarketPriceByModel(modelid));
+    new mileage_km = floatround(GetVehicleData(vehicleid, V_MILEAGE), floatround_floor);
+    mileage_penalty = mileage_km / 2500; // -1% every 2500 km
+    if(mileage_penalty > UVM_MAX_MILEAGE_PENALTY) mileage_penalty = UVM_MAX_MILEAGE_PENALTY;
 
-    ShowPlayerDialog(playerid, UVM_DIALOG_INFO, DIALOG_STYLE_MSGBOX,
-        "Vehicle passport", text, "Close", "");
-    return 1;
-}
+    wear_penalty = (g_uvm_car_wear[vehicleid] * UVM_MAX_WEAR_PENALTY) / UVM_WEAR_MAX;
+    if(wear_penalty > UVM_MAX_WEAR_PENALTY) wear_penalty = UVM_MAX_WEAR_PENALTY;
 
-stock UVM_ShowMarket(playerid)
-{
-    new query[512];
-    mysql_format(mysql, query, sizeof query,
-        "SELECT oc.id, oc.model_id, oc.number, oc.mileage, oc.car_wear, oc.insurance_until, oc.used_market_price, oc.owner_id, a.name "\
-        "FROM ownable_cars oc LEFT JOIN accounts a ON a.id=oc.owner_id "\
-        "WHERE oc.used_market_price > 0 ORDER BY oc.used_market_time DESC LIMIT %d",
-        UVM_MARKET_LIMIT);
+    new total_penalty = mileage_penalty + wear_penalty;
+    if(total_penalty > UVM_MAX_TOTAL_PENALTY) total_penalty = UVM_MAX_TOTAL_PENALTY;
 
-    new Cache:result = mysql_query(mysql, query, true);
-    new rows = cache_num_rows();
+    final_price = clean_sell_price - ((clean_sell_price * total_penalty) / 100);
 
-    if(!rows)
+    if(g_uvm_insurance_until[vehicleid] > gettime())
     {
-        cache_delete(result);
-        return SendClientMessage(playerid, 0xCECECEFF, "Used vehicle market is empty.");
+        insurance_bonus = (clean_sell_price * UVM_INSURANCE_BONUS) / 100;
+        final_price += insurance_bonus;
     }
+    else insurance_bonus = 0;
 
-    new content[4096];
-    format(content, sizeof content, "ID\tVehicle\tMileage\tWear\tPrice\tSeller\n");
-
-    new row[180], vehicle_name[64], number[32], seller_name[24];
-    for(new i; i < rows; i++)
-    {
-        new oc_id = cache_get_field_content_int(i, "id");
-        new modelid = cache_get_field_content_int(i, "model_id");
-        new price = cache_get_field_content_int(i, "used_market_price");
-        new wear = cache_get_field_content_int(i, "car_wear");
-        new Float:mileage = cache_get_field_content_float(i, "mileage");
-
-        cache_get_field_content(i, "number", number, mysql, sizeof number);
-        cache_get_field_content(i, "name", seller_name, mysql, sizeof seller_name);
+    if(final_price > clean_sell_price) final_price = clean_sell_price;
 
-        if(!GetVehicleJsonNameByModelId(modelid, vehicle_name, sizeof vehicle_name))
-            format(vehicle_name, sizeof vehicle_name, "Model %d", modelid);
+    new min_price = (base_price * UVM_MIN_GOV_SELL_PERCENT) / 100;
+    if(final_price < min_price) final_price = min_price;
 
-        format(row, sizeof row, "%d\t%s\t%d km\t%d%%\t%d\t%s\n",
-            oc_id, vehicle_name, floatround(mileage, floatround_floor), wear, price, seller_name);
-        strcat(content, row);
-        SetPlayerListitemValue(playerid, i, oc_id);
-    }
-
-    cache_delete(result);
-
-    ShowPlayerDialog(playerid, UVM_DIALOG_LIST, DIALOG_STYLE_TABLIST_HEADERS,
-        "Used vehicle market", content, "Info", "Close");
-    return 1;
+    return final_price;
 }
 
-stock UVM_ShowMarketInfo(playerid, oc_id)
+stock UVM_GetGovSellPrice(vehicleid)
 {
-    new query[512];
-    mysql_format(mysql, query, sizeof query,
-        "SELECT oc.*, a.name FROM ownable_cars oc LEFT JOIN accounts a ON a.id=oc.owner_id WHERE oc.id=%d AND oc.used_market_price > 0 LIMIT 1",
-        oc_id);
-
-    new Cache:result = mysql_query(mysql, query, true);
-    if(!cache_num_rows())
-    {
-        cache_delete(result);
-        return SendClientMessage(playerid, 0xCECECEFF, "This listing is no longer available.");
-    }
-
-    new modelid = cache_get_field_content_int(0, "model_id");
-    new price = cache_get_field_content_int(0, "used_market_price");
-    new owner_id = cache_get_field_content_int(0, "owner_id");
-    new wear = cache_get_field_content_int(0, "car_wear");
-    new previous_owners = cache_get_field_content_int(0, "previous_owners");
-    new insurance_until = cache_get_field_content_int(0, "insurance_until");
-    new Float:mileage = cache_get_field_content_float(0, "mileage");
-    new seller_name[24], vehicle_name[64], number[32], wear_text[24], insurance_text[32], content[1300];
-
-    cache_get_field_content(0, "name", seller_name, mysql, sizeof seller_name);
-    cache_get_field_content(0, "number", number, mysql, sizeof number);
-    cache_delete(result);
-
-    if(!GetVehicleJsonNameByModelId(modelid, vehicle_name, sizeof vehicle_name))
-        format(vehicle_name, sizeof vehicle_name, "Model %d", modelid);
-
-    UVM_GetWearText(wear, wear_text, sizeof wear_text);
-    UVM_FormatDaysLeft(insurance_until, insurance_text, sizeof insurance_text);
-
-    SetPVarInt(playerid, "uvm_buy_oc_id", oc_id);
-    SetPVarInt(playerid, "uvm_buy_price", price);
-
-    format(content, sizeof content,
-        "Vehicle:\t%s (#%d)\n"\
-        "Number:\t%s\n"\
-        "Seller:\t%s (acc %d)\n"\
-        "Mileage:\t%d km\n"\
-        "Wear:\t%d/100 (%s)\n"\
-        "Previous owners:\t%d\n"\
-        "Insurance:\t%s\n"\
-        "Price:\t%d rub\n\n"\
-        "Buy this vehicle?",
-        vehicle_name, modelid, number, seller_name, owner_id,
-        floatround(mileage, floatround_floor), wear, wear_text,
-        previous_owners, insurance_text, price);
-
-    ShowPlayerDialog(playerid, UVM_DIALOG_BUY_CONFIRM, DIALOG_STYLE_MSGBOX,
-        "Used vehicle info", content, "Buy", "Back");
-    return 1;
+    new base_price, clean_sell_price, mileage_penalty, wear_penalty, insurance_bonus, final_price;
+    UVM_GetGovSellPriceEx(vehicleid, base_price, clean_sell_price, mileage_penalty, wear_penalty, insurance_bonus, final_price);
+    return final_price;
 }
 
-stock UVM_BuyVehicle(playerid, oc_id)
+stock UVM_ShowGovSellDialog(playerid)
 {
-    if((GetPlayerOwnableCars(playerid) + 1) > GetPlayerCarSlots(playerid))
-        return SendClientMessage(playerid, 0xCECECEFF, "You do not have a free vehicle slot.");
-
-    if(GetPlayerOwnableCar(playerid) != INVALID_VEHICLE_ID)
-        return SendClientMessage(playerid, 0xCECECEFF, "Unload your current personal vehicle first. (/car)");
-
-    new query[512];
-    mysql_format(mysql, query, sizeof query,
-        "SELECT id, owner_id, model_id, used_market_price FROM ownable_cars WHERE id=%d AND used_market_price > 0 LIMIT 1",
-        oc_id);
-
-    new Cache:result = mysql_query(mysql, query, true);
-    if(!cache_num_rows())
+    new vehicleid = GetPlayerOwnableCar(playerid);
+    if(vehicleid == INVALID_VEHICLE_ID)
     {
-        cache_delete(result);
-        return SendClientMessage(playerid, 0xCECECEFF, "This listing is no longer available.");
+        if(GetPlayerOwnableCars(playerid) == 0)
+            return SendClientMessage(playerid, 0x999999FF, "You have no personal vehicle.");
+        return SendClientMessage(playerid, 0x999999FF, "Your personal vehicle is not loaded. Use /car first.");
     }
 
-    new owner_id = cache_get_field_content_int(0, "owner_id");
-    new price = cache_get_field_content_int(0, "used_market_price");
-    cache_delete(result);
-
-    if(owner_id == GetPlayerAccountID(playerid))
-        return SendClientMessage(playerid, 0xCECECEFF, "You cannot buy your own vehicle.");
+    if(!IsAOwnableCar(vehicleid)) return 1;
 
-    if(GetPlayerMoneyEx(playerid) < price)
-        return SendClientMessage(playerid, 0xCECECEFF, "Not enough money.");
+    new index = GetVehicleData(vehicleid, V_ACTION_ID);
+    if(index < 0) return 1;
 
-    new seller_money = price * UVM_SELLER_PERCENT / 100;
-    new sellerid = UVM_GetPlayerByAccount(owner_id);
+    if(GetOwnableCarData(index, OC_OWNER_ID) != GetPlayerAccountID(playerid))
+        return SendClientMessage(playerid, 0xCECECEFF, "This vehicle does not belong to you.");
 
-    new loaded_vehicle = UVM_FindLoadedVehicleBySql(oc_id);
-    if(loaded_vehicle != INVALID_VEHICLE_ID)
-    {
-        if(sellerid != INVALID_PLAYER_ID && GetPlayerOwnableCar(sellerid) == loaded_vehicle)
-        {
-            UnloadPlayerOwnableCar(sellerid);
-            SendClientMessage(sellerid, 0xFF9900FF, "Your listed vehicle was sold on the used market.");
-        }
-        else
-        {
-            DestroyVehicle(loaded_vehicle);
-        }
-    }
+    new modelid = GetVehicleData(vehicleid, V_MODELID);
+    new vehicle_name[64];
+    if(!GetVehicleJsonNameByModelId(modelid, vehicle_name, sizeof vehicle_name))
+        format(vehicle_name, sizeof vehicle_name, "Model %d", modelid);
 
-    GivePlayerMoneyEx(playerid, -price, "Used vehicle market purchase", true, true);
+    new base_price, clean_sell_price, mileage_penalty, wear_penalty, insurance_bonus, final_price;
+    UVM_GetGovSellPriceEx(vehicleid, base_price, clean_sell_price, mileage_penalty, wear_penalty, insurance_bonus, final_price);
 
-    if(sellerid != INVALID_PLAYER_ID)
-    {
-        GivePlayerMoneyEx(sellerid, seller_money, "Used vehicle market sale", true, true);
-    }
-    else
-    {
-        mysql_format(mysql, query, sizeof query,
-            "UPDATE accounts SET money=money+%d WHERE id=%d LIMIT 1",
-            seller_money, owner_id);
-        mysql_tquery(mysql, query, "", "");
-    }
+    new wear_text[24], insurance_text[32], text[1400];
+    UVM_GetWearText(g_uvm_car_wear[vehicleid], wear_text, sizeof wear_text);
+    UVM_FormatDaysLeft(g_uvm_insurance_until[vehicleid], insurance_text, sizeof insurance_text);
 
-    mysql_format(mysql, query, sizeof query,
-        "UPDATE ownable_cars SET owner_id=%d, used_market_price=0, used_market_seller=0, used_market_time=0, previous_owners=previous_owners+1 WHERE id=%d LIMIT 1",
-        GetPlayerAccountID(playerid), oc_id);
-    mysql_tquery(mysql, query, "", "");
+    format(text, sizeof text,
+        "Vehicle:\t%s (#%d)\n"\
+        "Gov. price:\t%d rub\n"\
+        "Clean gov. sale price:\t%d rub (%d%%)\n"\
+        "Mileage:\t%d km (-%d%%)\n"\
+        "Wear:\t%d/100, %s (-%d%%)\n"\
+        "Insurance:\t%s (+%d rub)\n\n"\
+        "Final gov. sale price:\t%d rub\n\n"\
+        "Vehicle will be deleted from your garage. Sell it to the state?",
+        vehicle_name,
+        modelid,
+        base_price,
+        clean_sell_price,
+        UVM_GOV_SELL_PERCENT,
+        floatround(GetVehicleData(vehicleid, V_MILEAGE), floatround_floor),
+        mileage_penalty,
+        g_uvm_car_wear[vehicleid],
+        wear_text,
+        wear_penalty,
+        insurance_text,
+        insurance_bonus,
+        final_price
+    );
 
-    SendClientMessage(playerid, 0x66CC00FF, "Vehicle purchased. Load it through /car.");
+    Dialog(playerid, DIALOG_OWNABLE_CAR_SELL, DIALOG_STYLE_MSGBOX,
+        "Vehicle sale to state", text, "Sell", "Cancel");
     return 1;
 }
 
-stock UVM_ListOwnVehicle(playerid, price)
+stock UVM_SellOwnVehicleToState(playerid)
 {
     new vehicleid = GetPlayerOwnableCar(playerid);
     if(vehicleid == INVALID_VEHICLE_ID)
-        return SendClientMessage(playerid, 0xCECECEFF, "Load your personal vehicle first. (/car)");
+        return SendClientMessage(playerid, 0xCECECEFF, "Your personal vehicle is not loaded. Use /car first.");
 
     if(!IsAOwnableCar(vehicleid)) return 1;
 
@@ -468,35 +324,29 @@
     if(GetOwnableCarData(index, OC_OWNER_ID) != GetPlayerAccountID(playerid))
         return SendClientMessage(playerid, 0xCECECEFF, "This vehicle does not belong to you.");
 
-    new modelid = GetVehicleData(vehicleid, V_MODELID);
-    new base_price = GetVehicleMarketPriceByModel(modelid);
-    if(base_price <= 0) base_price = 100000;
-
-    if(!(UVM_MIN_PRICE <= price <= base_price * 2))
-        return SendClientMessage(playerid, 0xCECECEFF, "Price must be from 3000 rub to 2x gov. vehicle price.");
-
-    new query[256];
-    new oc_id = GetOwnableCarData(index, OC_SQL_ID);
+    new sell_price = UVM_GetGovSellPrice(vehicleid);
 
     SaveOwnableCar(vehicleid);
     UVM_SaveRuntime(vehicleid);
 
-    mysql_format(mysql, query, sizeof query,
-        "UPDATE ownable_cars SET used_market_price=%d, used_market_seller=%d, used_market_time=%d WHERE id=%d LIMIT 1",
-        price, GetPlayerAccountID(playerid), gettime(), oc_id);
-    mysql_tquery(mysql, query, "", "");
+    if(DestroyOwnableCar(vehicleid))
+    {
+        new msg[192];
+        SetPlayerData(playerid, P_OWNABLE_CAR, INVALID_VEHICLE_ID);
+        GivePlayerMoneyEx(playerid, sell_price, "Vehicle sale to state", true, false);
 
-    UnloadPlayerOwnableCar(playerid);
+        format(msg, sizeof msg, "Vehicle sold to the state. You received %d rub.", sell_price);
+        SendClientMessage(playerid, 0x66CC00FF, msg);
+        return 1;
+    }
 
-    SendClientMessage(playerid, 0x66CC00FF, "Vehicle listed on used market. It was unloaded from the server.");
-    return 1;
+    return SendClientMessage(playerid, 0xCECECEFF, "Error: vehicle was not sold. Check server log / MySQL.");
 }
 
-stock UVM_UnlistOwnVehicle(playerid)
+stock UVM_ShowPassport(playerid, vehicleid)
 {
-    new vehicleid = GetPlayerOwnableCar(playerid);
-    if(vehicleid == INVALID_VEHICLE_ID)
-        return SendClientMessage(playerid, 0xCECECEFF, "Load your listed personal vehicle first, then use /delusedcar.");
+    if(vehicleid == INVALID_VEHICLE_ID || !IsAOwnableCar(vehicleid))
+        return SendClientMessage(playerid, 0xCECECEFF, "Load your personal vehicle first. (/car)");
 
     new index = GetVehicleData(vehicleid, V_ACTION_ID);
     if(index < 0) return 1;
@@ -504,13 +354,34 @@
     if(GetOwnableCarData(index, OC_OWNER_ID) != GetPlayerAccountID(playerid))
         return SendClientMessage(playerid, 0xCECECEFF, "This vehicle does not belong to you.");
 
-    new query[192];
-    mysql_format(mysql, query, sizeof query,
-        "UPDATE ownable_cars SET used_market_price=0, used_market_seller=0, used_market_time=0 WHERE id=%d LIMIT 1",
-        GetOwnableCarData(index, OC_SQL_ID));
-    mysql_tquery(mysql, query, "", "");
+    new modelid = GetVehicleData(vehicleid, V_MODELID);
+    new vehicle_name[64];
+    if(!GetVehicleJsonNameByModelId(modelid, vehicle_name, sizeof vehicle_name))
+        format(vehicle_name, sizeof vehicle_name, "Model %d", modelid);
 
-    SendClientMessage(playerid, 0x66CC00FF, "Vehicle removed from used market.");
+    new wear_text[24], insurance_text[32], text[1024];
+    UVM_GetWearText(g_uvm_car_wear[vehicleid], wear_text, sizeof wear_text);
+    UVM_FormatDaysLeft(g_uvm_insurance_until[vehicleid], insurance_text, sizeof insurance_text);
+
+    format(text, sizeof text,
+        "Vehicle:\t%s (#%d)\n"\
+        "DB ID:\t%d\n"\
+        "Mileage:\t%d km\n"\
+        "Wear:\t%d/100 (%s)\n"\
+        "Insurance:\t%s\n"\
+        "Insurance claims:\t%d\n"\
+        "Current /sellcar price:\t%d rub",
+        vehicle_name,
+        modelid,
+        GetOwnableCarData(index, OC_SQL_ID),
+        floatround(GetVehicleData(vehicleid, V_MILEAGE), floatround_floor),
+        g_uvm_car_wear[vehicleid],
+        wear_text,
+        insurance_text,
+        g_uvm_insurance_claims[vehicleid],
+        UVM_GetGovSellPrice(vehicleid));
+
+    ShowPlayerDialog(playerid, 28101, DIALOG_STYLE_MSGBOX, "Vehicle passport", text, "Close", "");
     return 1;
 }
 
@@ -620,18 +491,6 @@
 stock UVM_InitSql()
 {
     UVM_MigrateColumn(
-        "SELECT used_market_price FROM ownable_cars LIMIT 1",
-        "ALTER TABLE ownable_cars ADD used_market_price INT NOT NULL DEFAULT 0"
-    );
-    UVM_MigrateColumn(
-        "SELECT used_market_seller FROM ownable_cars LIMIT 1",
-        "ALTER TABLE ownable_cars ADD used_market_seller INT NOT NULL DEFAULT 0"
-    );
-    UVM_MigrateColumn(
-        "SELECT used_market_time FROM ownable_cars LIMIT 1",
-        "ALTER TABLE ownable_cars ADD used_market_time INT NOT NULL DEFAULT 0"
-    );
-    UVM_MigrateColumn(
         "SELECT car_wear FROM ownable_cars LIMIT 1",
         "ALTER TABLE ownable_cars ADD car_wear TINYINT UNSIGNED NOT NULL DEFAULT 0"
     );
@@ -643,53 +502,13 @@
         "SELECT insurance_claims FROM ownable_cars LIMIT 1",
         "ALTER TABLE ownable_cars ADD insurance_claims SMALLINT UNSIGNED NOT NULL DEFAULT 0"
     );
-    UVM_MigrateColumn(
-        "SELECT previous_owners FROM ownable_cars LIMIT 1",
-        "ALTER TABLE ownable_cars ADD previous_owners SMALLINT UNSIGNED NOT NULL DEFAULT 0"
-    );
     return 1;
 }
 
-CMD:usedcars(playerid, params[])
-{
-    return UVM_ShowMarket(playerid);
-}
-
-CMD:buyusedcar(playerid, params[])
-{
-    new oc_id;
-    if(sscanf(params, "d", oc_id)) return SendClientMessage(playerid, 0xCECECEFF, "Use: /buyusedcar [vehicle DB id]");
-    return UVM_BuyVehicle(playerid, oc_id);
-}
-
-CMD:sellusedcar(playerid, params[])
-{
-    new price;
-    if(sscanf(params, "d", price)) return SendClientMessage(playerid, 0xCECECEFF, "Use: /sellusedcar [price]");
-    return UVM_ListOwnVehicle(playerid, price);
-}
-
-CMD:delusedcar(playerid, params[])
-{
-    return UVM_UnlistOwnVehicle(playerid);
-}
-
-CMD:carpassport(playerid, params[])
-{
-    return UVM_ShowPassport(playerid, GetPlayerOwnableCar(playerid));
-}
-
-CMD:buyinsurance(playerid, params[])
-{
-    new days;
-    if(sscanf(params, "d", days)) return SendClientMessage(playerid, 0xCECECEFF, "Use: /buyinsurance [1-30 days]");
-    return UVM_BuyInsurance(playerid, days);
-}
-
-CMD:repairwear(playerid, params[])
-{
-    return UVM_RepairWear(playerid);
-}
+// No extra commands are registered here.
+// /sellcar is handled by the patched command in laird.pwn.
+// UVM_ShowPassport, UVM_BuyInsurance and UVM_RepairWear are left as stocks
+// so you can later connect them to your existing /car menu or NPC dialogs.
 
 stock UVM_CreateVehicle(modelid, Float:x, Float:y, Float:z, Float:angle, color1, color2, respawn_delay, addsiren=0, action_type=VEHICLE_ACTION_TYPE_NONE, action_id=VEHICLE_ACTION_ID_NONE)
 {
@@ -725,12 +544,14 @@
 
 public OnGameModeInit()
 {
-    UVM_InitSql();
 #if defined UVM_OnGameModeInit
-    return UVM_OnGameModeInit();
+    new result = UVM_OnGameModeInit();
 #else
-    return 1;
+    new result = 1;
 #endif
+    // Run SQL migration AFTER the original OnGameModeInit, because mysql is connected there.
+    UVM_InitSql();
+    return result;
 }
 #if defined _ALS_OnGameModeInit
     #undef OnGameModeInit
@@ -809,37 +630,3 @@
 #if defined UVM_OnVehicleDeath
 forward UVM_OnVehicleDeath(vehicleid, killerid);
 #endif
-
-public OnDialogResponse(playerid, dialogid, response, listitem, inputtext[])
-{
-    switch(dialogid)
-    {
-        case UVM_DIALOG_LIST:
-        {
-            if(!response) return 1;
-            new oc_id = GetPlayerListitemValue(playerid, listitem);
-            return UVM_ShowMarketInfo(playerid, oc_id);
-        }
-        case UVM_DIALOG_BUY_CONFIRM:
-        {
-            if(!response) return UVM_ShowMarket(playerid);
-            return UVM_BuyVehicle(playerid, GetPVarInt(playerid, "uvm_buy_oc_id"));
-        }
-    }
-
-#if defined UVM_OnDialogResponse
-    return UVM_OnDialogResponse(playerid, dialogid, response, listitem, inputtext);
-#else
-    return 0;
-#endif
-}
-#if defined _ALS_OnDialogResponse
-    #undef OnDialogResponse
-#else
-    #define _ALS_OnDialogResponse
-#endif
-#define OnDialogResponse UVM_OnDialogResponse
-#if defined UVM_OnDialogResponse
-forward UVM_OnDialogResponse(playerid, dialogid, response, listitem, inputtext[]);
-#endif
-
