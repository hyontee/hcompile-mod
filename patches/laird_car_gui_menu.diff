--- /mnt/data/used_vehicle_govsell_ru_patch/gamemodes/laird.pwn	2026-06-16 13:03:05.220978060 +0000
+++ /mnt/data/car_gui_menu_patch/gamemodes/laird.pwn	2026-06-16 13:42:59.815259285 +0000
@@ -3506,6 +3506,10 @@
 	DIALOG_OWNABLE_CAR_LOAD,
 	DIALOG_OWNABLE_CAR_LIST,
 	DIALOG_OWNABLE_CAR_SELL,
+	DIALOG_CAR_GUI_MAIN,
+	DIALOG_CAR_GUI_INSURANCE,
+	DIALOG_CAR_GUI_REPAIR_CONFIRM,
+	DIALOG_CAR_GUI_UNLOAD_CONFIRM,
 	DIALOG_FAMILY_CAR_SELL,
 	DIALOG_OWNABLE_CAR_NUMBER,
 	DIALOG_OWNABLE_CAR_PT,
@@ -7618,6 +7622,7 @@
 #include "../include/system/blackpass_rewards.inc"
 #include "../include/system/blackpass.inc"
 #include "../include/system/used_vehicle_market_ext.inc"
+#include "../include/system/car_gui_menu.inc"
 #include "../gamemodes/gamegui/death_gui.inc"
 #include "../gamemodes/sile/ipacket.inc"
@@ -14142,6 +14147,7 @@
     if(AZC_HandleDialog(playerid, dialogid, response, listitem, inputtext)) return 1;
     if(BlackMarket_HandleDialog(playerid, dialogid, response, listitem, inputtext)) return 1;
     if(Trade_HandleDialog(playerid, dialogid, response, listitem, inputtext)) return 1;
+    if(CarGUI_HandleDialog(playerid, dialogid, response, listitem, inputtext)) return 1;
 	if(dialogid == DIALOG_ADM_STDI)
 	{
 		if(response)
@@ -55059,21 +55065,7 @@
 
     if (ownableCar != INVALID_VEHICLE_ID)
     {
-        Dialog(playerid, DIALOG_OWNABLE_CAR, DIALOG_STYLE_LIST,
-            "{FFCD00}Система управления транспортом",
-            "1. {669900}Открыть {FFFFFF}или {FF3300}закрыть {FFFFFF}транспорт\n"\
-            "2. Отметить транспорт на GPS {FF6600}(300 руб)\n"\
-            "3. Содержимое багажника\n"\
-            "4. Документы на транспорт\n"\
-            "5. Снять номерные знаки\n"\
-            "6. Припарковать транспорт\n"\
-            "{888888}7. Выгрузить транспорт с сервера\n"\
-            "8. Установить сабвуфер в авто\n"\
-            "9. Снять сабвуфер с авто\n"\
-            "10. Добавить авто в семью",
-            "Выбрать", "Закрыть"
-        );
-        return 1;
+        return CarGUI_ShowMain(playerid);
     }
 
     new fmt_text[512];
