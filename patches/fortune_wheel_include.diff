--- work_wheel/include/system/fortune_wheel_gui.inc	1970-01-01 00:00:00.000000000 +0000
+++ fortune_wheel_gui_patch/include/system/fortune_wheel_gui.inc	2026-06-16 19:07:32.582553859 +0000
@@ -0,0 +1,412 @@
+/*
+    GUI-колесо фортуны для laird.pwn
+
+    Особенности:
+    - настоящий визуальный прокрут через PlayerTextDraw;
+    - подсветка бегает по секторам и постепенно замедляется;
+    - приз выдаётся только после полной остановки;
+    - нет обычного диалога с мгновенным рандомом;
+    - команды: /wheel, /fortune, /fortuna, /koleso.
+*/
+
+#if defined _fortune_wheel_gui_included
+    #endinput
+#endif
+#define _fortune_wheel_gui_included
+
+#define FORTUNE_TD_COUNT               (14)
+#define FORTUNE_PRIZES                 (8)
+#define FORTUNE_SPIN_PRICE             (50000)
+#define FORTUNE_MIN_TICKS              (28)
+#define FORTUNE_MAX_EXTRA_TICKS        (18)
+
+#define FORTUNE_COLOR_MAIN             (0xFFCD00FF)
+#define FORTUNE_COLOR_WHITE            (0xFFFFFFFF)
+#define FORTUNE_COLOR_GRAY             (0xCECECEFF)
+#define FORTUNE_COLOR_RED              (0xFF5252FF)
+#define FORTUNE_COLOR_GREEN            (0x66CC33FF)
+#define FORTUNE_COLOR_BG               (0x101010DD)
+#define FORTUNE_COLOR_BOX              (0x222222CC)
+#define FORTUNE_COLOR_HILITE           (0xFFCD00DD)
+
+#define FORTUNE_TD_BG                  (0)
+#define FORTUNE_TD_TITLE               (1)
+#define FORTUNE_TD_HINT                (2)
+#define FORTUNE_TD_CENTER              (3)
+#define FORTUNE_TD_SPIN                (4)
+#define FORTUNE_TD_CLOSE               (5)
+#define FORTUNE_TD_PRIZE_START         (6)
+
+enum E_FORTUNE_REWARD_TYPE
+{
+    FORTUNE_REWARD_MONEY = 1,
+    FORTUNE_REWARD_EXP,
+    FORTUNE_REWARD_DONATE,
+    FORTUNE_REWARD_CASE
+};
+
+enum E_FORTUNE_PRIZE
+{
+    FORTUNE_PRIZE_NAME[32],
+    FORTUNE_PRIZE_TYPE,
+    FORTUNE_PRIZE_VALUE,
+    FORTUNE_PRIZE_CHANCE
+};
+
+new const g_fortune_prize[][E_FORTUNE_PRIZE] =
+{
+    {"15 000 руб.",        FORTUNE_REWARD_MONEY,  15000, 240},
+    {"50 000 руб.",        FORTUNE_REWARD_MONEY,  50000, 170},
+    {"100 000 руб.",       FORTUNE_REWARD_MONEY, 100000,  90},
+    {"250 000 руб.",       FORTUNE_REWARD_MONEY, 250000,  35},
+    {"5 EXP",              FORTUNE_REWARD_EXP,       5, 190},
+    {"15 донат-монет",     FORTUNE_REWARD_DONATE,   15,  85},
+    {"1 кейс",             FORTUNE_REWARD_CASE,      1,  55},
+    {"ДЖЕКПОТ 500 000",    FORTUNE_REWARD_MONEY, 500000,  15}
+};
+
+new Float:g_fortune_pos_x[FORTUNE_PRIZES] =
+{
+    282.0, 358.0, 390.0, 358.0, 282.0, 206.0, 174.0, 206.0
+};
+
+new Float:g_fortune_pos_y[FORTUNE_PRIZES] =
+{
+    112.0, 135.0, 192.0, 249.0, 272.0, 249.0, 192.0, 135.0
+};
+
+new PlayerText:g_fortune_td[MAX_PLAYERS][FORTUNE_TD_COUNT];
+new bool:g_fortune_created[MAX_PLAYERS];
+new bool:g_fortune_open[MAX_PLAYERS];
+new bool:g_fortune_spinning[MAX_PLAYERS];
+new g_fortune_timer[MAX_PLAYERS];
+new g_fortune_index[MAX_PLAYERS];
+new g_fortune_tick[MAX_PLAYERS];
+new g_fortune_total_ticks[MAX_PLAYERS];
+new g_fortune_target[MAX_PLAYERS];
+new g_fortune_delay[MAX_PLAYERS];
+
+stock Fortune_CreateBox(playerid, idx, Float:x, Float:y, Float:w, Float:h, const text[], color, box_color, selectable = 0)
+{
+    g_fortune_td[playerid][idx] = CreatePlayerTextDraw(playerid, x, y, text);
+    PlayerTextDrawLetterSize(playerid, g_fortune_td[playerid][idx], 0.230000, 1.120000);
+    PlayerTextDrawTextSize(playerid, g_fortune_td[playerid][idx], x + w, h);
+    PlayerTextDrawAlignment(playerid, g_fortune_td[playerid][idx], 2);
+    PlayerTextDrawColor(playerid, g_fortune_td[playerid][idx], color);
+    PlayerTextDrawUseBox(playerid, g_fortune_td[playerid][idx], 1);
+    PlayerTextDrawBoxColor(playerid, g_fortune_td[playerid][idx], box_color);
+    PlayerTextDrawSetShadow(playerid, g_fortune_td[playerid][idx], 0);
+    PlayerTextDrawSetOutline(playerid, g_fortune_td[playerid][idx], 1);
+    PlayerTextDrawBackgroundColor(playerid, g_fortune_td[playerid][idx], 0x000000FF);
+    PlayerTextDrawFont(playerid, g_fortune_td[playerid][idx], 1);
+    PlayerTextDrawSetProportional(playerid, g_fortune_td[playerid][idx], 1);
+    PlayerTextDrawSetSelectable(playerid, g_fortune_td[playerid][idx], selectable);
+    return 1;
+}
+
+stock Fortune_CreateTextDraws(playerid)
+{
+    if(g_fortune_created[playerid]) return 1;
+
+    g_fortune_td[playerid][FORTUNE_TD_BG] = CreatePlayerTextDraw(playerid, 320.0, 86.0, "_");
+    PlayerTextDrawLetterSize(playerid, g_fortune_td[playerid][FORTUNE_TD_BG], 0.0, 23.8);
+    PlayerTextDrawTextSize(playerid, g_fortune_td[playerid][FORTUNE_TD_BG], 510.0, 0.0);
+    PlayerTextDrawAlignment(playerid, g_fortune_td[playerid][FORTUNE_TD_BG], 2);
+    PlayerTextDrawColor(playerid, g_fortune_td[playerid][FORTUNE_TD_BG], FORTUNE_COLOR_WHITE);
+    PlayerTextDrawUseBox(playerid, g_fortune_td[playerid][FORTUNE_TD_BG], 1);
+    PlayerTextDrawBoxColor(playerid, g_fortune_td[playerid][FORTUNE_TD_BG], FORTUNE_COLOR_BG);
+    PlayerTextDrawSetShadow(playerid, g_fortune_td[playerid][FORTUNE_TD_BG], 0);
+    PlayerTextDrawSetOutline(playerid, g_fortune_td[playerid][FORTUNE_TD_BG], 0);
+    PlayerTextDrawBackgroundColor(playerid, g_fortune_td[playerid][FORTUNE_TD_BG], 0x000000FF);
+    PlayerTextDrawFont(playerid, g_fortune_td[playerid][FORTUNE_TD_BG], 1);
+    PlayerTextDrawSetProportional(playerid, g_fortune_td[playerid][FORTUNE_TD_BG], 1);
+    PlayerTextDrawSetSelectable(playerid, g_fortune_td[playerid][FORTUNE_TD_BG], 0);
+
+    g_fortune_td[playerid][FORTUNE_TD_TITLE] = CreatePlayerTextDraw(playerid, 320.0, 92.0, "КОЛЕСО ФОРТУНЫ");
+    PlayerTextDrawLetterSize(playerid, g_fortune_td[playerid][FORTUNE_TD_TITLE], 0.360000, 1.600000);
+    PlayerTextDrawAlignment(playerid, g_fortune_td[playerid][FORTUNE_TD_TITLE], 2);
+    PlayerTextDrawColor(playerid, g_fortune_td[playerid][FORTUNE_TD_TITLE], FORTUNE_COLOR_MAIN);
+    PlayerTextDrawSetShadow(playerid, g_fortune_td[playerid][FORTUNE_TD_TITLE], 0);
+    PlayerTextDrawSetOutline(playerid, g_fortune_td[playerid][FORTUNE_TD_TITLE], 1);
+    PlayerTextDrawBackgroundColor(playerid, g_fortune_td[playerid][FORTUNE_TD_TITLE], 0x000000FF);
+    PlayerTextDrawFont(playerid, g_fortune_td[playerid][FORTUNE_TD_TITLE], 1);
+    PlayerTextDrawSetProportional(playerid, g_fortune_td[playerid][FORTUNE_TD_TITLE], 1);
+    PlayerTextDrawSetSelectable(playerid, g_fortune_td[playerid][FORTUNE_TD_TITLE], 0);
+
+    g_fortune_td[playerid][FORTUNE_TD_HINT] = CreatePlayerTextDraw(playerid, 320.0, 314.0, "Цена прокрута: 50000 руб.~n~Нажмите ПРОКРУТ, колесо само замедлится и остановится на призе");
+    PlayerTextDrawLetterSize(playerid, g_fortune_td[playerid][FORTUNE_TD_HINT], 0.220000, 1.050000);
+    PlayerTextDrawAlignment(playerid, g_fortune_td[playerid][FORTUNE_TD_HINT], 2);
+    PlayerTextDrawColor(playerid, g_fortune_td[playerid][FORTUNE_TD_HINT], FORTUNE_COLOR_GRAY);
+    PlayerTextDrawSetShadow(playerid, g_fortune_td[playerid][FORTUNE_TD_HINT], 0);
+    PlayerTextDrawSetOutline(playerid, g_fortune_td[playerid][FORTUNE_TD_HINT], 1);
+    PlayerTextDrawBackgroundColor(playerid, g_fortune_td[playerid][FORTUNE_TD_HINT], 0x000000FF);
+    PlayerTextDrawFont(playerid, g_fortune_td[playerid][FORTUNE_TD_HINT], 1);
+    PlayerTextDrawSetProportional(playerid, g_fortune_td[playerid][FORTUNE_TD_HINT], 1);
+    PlayerTextDrawSetSelectable(playerid, g_fortune_td[playerid][FORTUNE_TD_HINT], 0);
+
+    Fortune_CreateBox(playerid, FORTUNE_TD_CENTER, 320.0, 190.0, 80.0, 28.0, "ВЫБЕРИ~n~УДАЧУ", FORTUNE_COLOR_WHITE, 0x000000CC, 0);
+    Fortune_CreateBox(playerid, FORTUNE_TD_SPIN, 267.0, 346.0, 105.0, 23.0, "ПРОКРУТ", FORTUNE_COLOR_WHITE, 0x2E8B57DD, 1);
+    Fortune_CreateBox(playerid, FORTUNE_TD_CLOSE, 373.0, 346.0, 105.0, 23.0, "ЗАКРЫТЬ", FORTUNE_COLOR_WHITE, 0x8B0000DD, 1);
+
+    for(new i; i < FORTUNE_PRIZES; i++)
+    {
+        Fortune_CreateBox(playerid,
+            FORTUNE_TD_PRIZE_START + i,
+            g_fortune_pos_x[i],
+            g_fortune_pos_y[i],
+            92.0,
+            20.0,
+            g_fortune_prize[i][FORTUNE_PRIZE_NAME],
+            FORTUNE_COLOR_WHITE,
+            FORTUNE_COLOR_BOX,
+            0
+        );
+    }
+
+    g_fortune_created[playerid] = true;
+    return 1;
+}
+
+stock Fortune_ShowTD(playerid, idx)
+{
+    PlayerTextDrawShow(playerid, g_fortune_td[playerid][idx]);
+    return 1;
+}
+
+stock Fortune_Show(playerid)
+{
+    if(g_fortune_spinning[playerid]) return SendClientMessage(playerid, FORTUNE_COLOR_GRAY, "[Колесо] Дождитесь окончания текущего прокрута.");
+
+    Fortune_CreateTextDraws(playerid);
+
+    for(new i; i < FORTUNE_TD_COUNT; i++)
+    {
+        Fortune_ShowTD(playerid, i);
+    }
+
+    g_fortune_open[playerid] = true;
+    g_fortune_index[playerid] = 0;
+    Fortune_SetHighlight(playerid, 0);
+    SelectTextDraw(playerid, FORTUNE_COLOR_MAIN);
+    return 1;
+}
+
+stock Fortune_Hide(playerid)
+{
+    if(!g_fortune_created[playerid]) return 1;
+
+    for(new i; i < FORTUNE_TD_COUNT; i++)
+    {
+        PlayerTextDrawHide(playerid, g_fortune_td[playerid][i]);
+    }
+
+    g_fortune_open[playerid] = false;
+    CancelSelectTextDraw(playerid);
+    return 1;
+}
+
+stock Fortune_Destroy(playerid)
+{
+    if(g_fortune_timer[playerid])
+    {
+        KillTimer(g_fortune_timer[playerid]);
+        g_fortune_timer[playerid] = 0;
+    }
+
+    if(g_fortune_created[playerid])
+    {
+        for(new i; i < FORTUNE_TD_COUNT; i++)
+        {
+            PlayerTextDrawDestroy(playerid, g_fortune_td[playerid][i]);
+        }
+    }
+
+    g_fortune_created[playerid] = false;
+    g_fortune_open[playerid] = false;
+    g_fortune_spinning[playerid] = false;
+    return 1;
+}
+
+stock Fortune_ResetHighlight(playerid)
+{
+    for(new i; i < FORTUNE_PRIZES; i++)
+    {
+        PlayerTextDrawBoxColor(playerid, g_fortune_td[playerid][FORTUNE_TD_PRIZE_START + i], FORTUNE_COLOR_BOX);
+        PlayerTextDrawHide(playerid, g_fortune_td[playerid][FORTUNE_TD_PRIZE_START + i]);
+        PlayerTextDrawShow(playerid, g_fortune_td[playerid][FORTUNE_TD_PRIZE_START + i]);
+    }
+    return 1;
+}
+
+stock Fortune_SetHighlight(playerid, prizeid)
+{
+    Fortune_ResetHighlight(playerid);
+    PlayerTextDrawBoxColor(playerid, g_fortune_td[playerid][FORTUNE_TD_PRIZE_START + prizeid], FORTUNE_COLOR_HILITE);
+    PlayerTextDrawHide(playerid, g_fortune_td[playerid][FORTUNE_TD_PRIZE_START + prizeid]);
+    PlayerTextDrawShow(playerid, g_fortune_td[playerid][FORTUNE_TD_PRIZE_START + prizeid]);
+    return 1;
+}
+
+stock Fortune_SelectPrize()
+{
+    new total_chance;
+    for(new i; i < FORTUNE_PRIZES; i++) total_chance += g_fortune_prize[i][FORTUNE_PRIZE_CHANCE];
+
+    new random_value = random(total_chance);
+    new current_sum;
+
+    for(new i; i < FORTUNE_PRIZES; i++)
+    {
+        current_sum += g_fortune_prize[i][FORTUNE_PRIZE_CHANCE];
+        if(random_value < current_sum) return i;
+    }
+
+    return 0;
+}
+
+stock Fortune_GiveReward(playerid, prizeid)
+{
+    new reward_text[144];
+
+    switch(g_fortune_prize[prizeid][FORTUNE_PRIZE_TYPE])
+    {
+        case FORTUNE_REWARD_MONEY:
+        {
+            GivePlayerMoneyEx(playerid, g_fortune_prize[prizeid][FORTUNE_PRIZE_VALUE], "Колесо фортуны", true, true);
+            format(reward_text, sizeof reward_text, "[Колесо] Поздравляем! Ваш приз: %s.", g_fortune_prize[prizeid][FORTUNE_PRIZE_NAME]);
+        }
+        case FORTUNE_REWARD_EXP:
+        {
+            AddPlayerData(playerid, P_EXP, +, g_fortune_prize[prizeid][FORTUNE_PRIZE_VALUE]);
+            UpdatePlayerDatabaseInt(playerid, "exp", GetPlayerData(playerid, P_EXP));
+            format(reward_text, sizeof reward_text, "[Колесо] Поздравляем! Ваш приз: %s.", g_fortune_prize[prizeid][FORTUNE_PRIZE_NAME]);
+        }
+        case FORTUNE_REWARD_DONATE:
+        {
+            GivePlayerDonate(playerid, g_fortune_prize[prizeid][FORTUNE_PRIZE_VALUE], "Колесо фортуны", true);
+            format(reward_text, sizeof reward_text, "[Колесо] Поздравляем! Ваш приз: %s.", g_fortune_prize[prizeid][FORTUNE_PRIZE_NAME]);
+        }
+        case FORTUNE_REWARD_CASE:
+        {
+            AddPlayerData(playerid, P_DONATE_CASE, +, g_fortune_prize[prizeid][FORTUNE_PRIZE_VALUE]);
+            UpdatePlayerDatabaseInt(playerid, "case", GetPlayerData(playerid, P_DONATE_CASE));
+            format(reward_text, sizeof reward_text, "[Колесо] Поздравляем! Ваш приз: %s.", g_fortune_prize[prizeid][FORTUNE_PRIZE_NAME]);
+        }
+    }
+
+    SendClientMessage(playerid, FORTUNE_COLOR_GREEN, reward_text);
+    format(reward_text, sizeof reward_text, "Выпало: %s~n~Нажмите ПРОКРУТ, чтобы сыграть ещё раз", g_fortune_prize[prizeid][FORTUNE_PRIZE_NAME]);
+    PlayerTextDrawSetString(playerid, g_fortune_td[playerid][FORTUNE_TD_HINT], reward_text);
+    return 1;
+}
+
+stock Fortune_StartSpin(playerid)
+{
+    if(!g_fortune_open[playerid]) return 1;
+    if(g_fortune_spinning[playerid]) return SendClientMessage(playerid, FORTUNE_COLOR_GRAY, "[Колесо] Прокрут уже идёт.");
+
+    if(GetPlayerMoneyEx(playerid) < FORTUNE_SPIN_PRICE)
+    {
+        new msg[128];
+        format(msg, sizeof msg, "[Колесо] Для прокрута нужно %d руб.", FORTUNE_SPIN_PRICE);
+        return SendClientMessage(playerid, FORTUNE_COLOR_RED, msg);
+    }
+
+    GivePlayerMoneyEx(playerid, -FORTUNE_SPIN_PRICE, "Прокрут колеса фортуны", true, true);
+
+    g_fortune_spinning[playerid] = true;
+    g_fortune_target[playerid] = Fortune_SelectPrize();
+    g_fortune_tick[playerid] = 0;
+    g_fortune_index[playerid] = 0;
+    g_fortune_delay[playerid] = 45;
+    g_fortune_total_ticks[playerid] = FORTUNE_MIN_TICKS + random(FORTUNE_MAX_EXTRA_TICKS);
+
+    while((g_fortune_total_ticks[playerid] % FORTUNE_PRIZES) != g_fortune_target[playerid])
+    {
+        g_fortune_total_ticks[playerid]++;
+    }
+
+    PlayerTextDrawSetString(playerid, g_fortune_td[playerid][FORTUNE_TD_HINT], "Колесо крутится...~n~Дождитесь полной остановки");
+    g_fortune_timer[playerid] = SetTimerEx("Fortune_SpinTick", g_fortune_delay[playerid], false, "i", playerid);
+    return 1;
+}
+
+forward Fortune_SpinTick(playerid);
+public Fortune_SpinTick(playerid)
+{
+    if(!IsPlayerConnected(playerid) || !g_fortune_spinning[playerid]) return 1;
+
+    g_fortune_index[playerid] = (g_fortune_index[playerid] + 1) % FORTUNE_PRIZES;
+    g_fortune_tick[playerid]++;
+    Fortune_SetHighlight(playerid, g_fortune_index[playerid]);
+
+    if(g_fortune_tick[playerid] >= g_fortune_total_ticks[playerid])
+    {
+        g_fortune_timer[playerid] = 0;
+        g_fortune_spinning[playerid] = false;
+        Fortune_SetHighlight(playerid, g_fortune_target[playerid]);
+        Fortune_GiveReward(playerid, g_fortune_target[playerid]);
+        return 1;
+    }
+
+    if(g_fortune_tick[playerid] > 10) g_fortune_delay[playerid] += 9;
+    if(g_fortune_tick[playerid] > 20) g_fortune_delay[playerid] += 13;
+    if(g_fortune_tick[playerid] > 30) g_fortune_delay[playerid] += 18;
+
+    if(g_fortune_delay[playerid] > 420) g_fortune_delay[playerid] = 420;
+    g_fortune_timer[playerid] = SetTimerEx("Fortune_SpinTick", g_fortune_delay[playerid], false, "i", playerid);
+    return 1;
+}
+
+stock Fortune_OnPlayerClickPlayerTextDraw(playerid, PlayerText:playertextid)
+{
+    if(!g_fortune_open[playerid]) return 0;
+
+    if(playertextid == g_fortune_td[playerid][FORTUNE_TD_SPIN])
+    {
+        Fortune_StartSpin(playerid);
+        return 1;
+    }
+
+    if(playertextid == g_fortune_td[playerid][FORTUNE_TD_CLOSE])
+    {
+        if(g_fortune_spinning[playerid])
+        {
+            SendClientMessage(playerid, FORTUNE_COLOR_GRAY, "[Колесо] Нельзя закрыть меню во время прокрута.");
+            return 1;
+        }
+        Fortune_Hide(playerid);
+        return 1;
+    }
+
+    return 0;
+}
+
+stock Fortune_Cleanup(playerid)
+{
+    return Fortune_Destroy(playerid);
+}
+
+CMD:wheel(playerid, params[])
+{
+    #pragma unused params
+    return Fortune_Show(playerid);
+}
+
+CMD:fortune(playerid, params[])
+{
+    #pragma unused params
+    return Fortune_Show(playerid);
+}
+
+CMD:fortuna(playerid, params[])
+{
+    #pragma unused params
+    return Fortune_Show(playerid);
+}
+
+CMD:koleso(playerid, params[])
+{
+    #pragma unused params
+    return Fortune_Show(playerid);
+}
