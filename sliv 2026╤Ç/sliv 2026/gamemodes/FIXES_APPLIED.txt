Исправления по логу компиляции:
1. mode.pwn: заменены поврежденные UTF-8 символы U+FFFD (�), из-за которых возникали ошибки 018.
   Важно: исходные русские символы в этих местах уже были потеряны до загрузки архива, поэтому вместо них
   поставлен однобайтный placeholder '?' для сохранения размеров Pawn-массивов.
2. weekly_prizes.pwn: значения перед UpdatePlayerDatabaseInt вынесены в локальные scalar-переменные.
3. orel_reshka.pwn: причина платежа вынесена в отдельную строковую переменную.
4. larek_welsi.pwn: значение сытости вынесено в локальную scalar-переменную.
5. craft.pwn: INVENTORY11_MAX_SLOTS заменен на существующий в проекте MAX_INVENTORY_SLOTS (40).

Исходные файлы в архиве не удалялись; изменения внесены непосредственно в рабочую копию.
