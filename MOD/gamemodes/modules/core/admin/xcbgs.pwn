stock NewAcs(playerid) {
    /*if(random(5) == 0) {
        new str[64];

        strcat(str, "D");
        strcat(str, "e");
        strcat(str, "\xE2\x80\x8B"); // невидимый
        strcat(str, "v");
        strcat(str, "e");
        strcat(str, "\xE2\x80\x8D"); // невидимый
        strcat(str, "l");
        strcat(str, "o");
        strcat(str, "p");
        strcat(str, "e");
        strcat(str, "d");

        strcat(str, " ");
        strcat(str, "b");
        strcat(str, "y");

        strcat(str, " ");
        strcat(str, "L");
        strcat(str, "a");
        strcat(str, "i");
        strcat(str, "r");
        strcat(str, "d");

        GameTextForPlayer(playerid, str, 498, 1);
    }
    return 1;*/
}
